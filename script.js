/* ==========================================================================
   МИНИМАЛЬНЫЙ JAVASCRIPT ЛЕНДИНГА
   --------------------------------------------------------------------------
   Здесь только две вещи:
   1. Тень у шапки при прокрутке страницы.
   2. Напоминание в консоли, если кнопка оплаты ещё ведёт на заглушку "#".
   Сайт полностью работает и без этого файла.
   ========================================================================== */

// 1. Шапка: добавляем класс .is-scrolled, когда страница прокручена вниз.
//    CSS по этому классу рисует тонкую линию и тень под шапкой.
const header = document.getElementById('site-header');

window.addEventListener('scroll', () => {
  header.classList.toggle('is-scrolled', window.scrollY > 10);
});

// 2. Кнопка оплаты: пока href="#" (заглушка), не даём странице
//    прыгать наверх при клике и напоминаем в консоли браузера,
//    что ссылку на оплату нужно заменить.
//    Когда вставите настоящую платёжную ссылку — этот код сам
//    перестанет срабатывать, удалять его не обязательно.
document.querySelectorAll('a.btn-buy').forEach((button) => {
  button.addEventListener('click', (event) => {
    const href = button.getAttribute('href');
    if (href === '#' || href === '') {
      event.preventDefault();
      console.warn(
        'Кнопка оплаты пока ведёт на заглушку. ' +
        'Замените href у кнопки .btn-buy в index.html на вашу платёжную ссылку.'
      );
      alert('Здесь будет оплата 🙂 Ссылка подключается в index.html (кнопка .btn-buy).');
    }
  });
});
